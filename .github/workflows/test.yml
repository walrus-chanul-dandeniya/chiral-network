name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    name: CI Pipeline
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22.12"

      - name: Cache Node modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev libsoup-3.0-dev \
            libglib2.0-dev pkg-config build-essential libgtk-3-dev curl libssl-dev \
            libayatana-appindicator3-dev librsvg2-dev

      - name: Install Node dependencies
        run: npm install

      - name: Run frontend linting
        run: npm run check || true

      - name: Build frontend
        run: npm run build

      - name: Build backend
        run: cd src-tauri && cargo build --release

      - name: Run NAT traversal settings tests
        run: cd src-tauri && cargo test --test nat_traversal_test --release
        continue-on-error: false

      - name: Run NAT traversal E2E tests
        run: cd src-tauri && cargo test --test nat_traversal_e2e_test --release -- --test-threads=1
        continue-on-error: true
        timeout-minutes: 10

      - name: Run relay tests
        run: cd relay && cargo test --release
        continue-on-error: true
        timeout-minutes: 5
